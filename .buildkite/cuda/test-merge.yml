# L3 merge — Simple Test + E2E. Uploaded by cuda/bootstrap-upload-steps.yml.
steps:
  - group: ":card_index_dividers: Simple Test"
    depends_on: upload-merge-pipeline
    steps:
      - label: "Simple · Unit Test"
        timeout_in_minutes: 20
        commands:
          - 'pytest -sv tests/unit -m "not gpu and not vllm_runtime"'
        mirror_hardwares: l4_1

  - group: ":card_index_dividers: E2E Test"
    depends_on: upload-merge-pipeline
    steps:
      - label: "E2E · DeepSeekV2-Lite"
        timeout_in_minutes: 40
        commands:
          - "pip install 'lm_eval[api]==0.4.12'"
          - "export AFD_E2E_BACKEND=gpu"
          - "pytest -sv tests/e2e/models/deepseek_v2_lite/test_deepseek_v2_lite.py"
        mirror_hardwares: l4_4
